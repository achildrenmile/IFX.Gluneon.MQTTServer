syntax = "proto3";
package gluneonio;

// RawMessage represents a message emitted by the Mainflux adapters layer.
message RawMessage {
	string  Project     = 1;
	string  Publisher   = 2;
	string  Protocol    = 3;
	string  ContentType = 4;
	string  DefinedTopic = 5;
	bytes   Payload     = 6;
}
